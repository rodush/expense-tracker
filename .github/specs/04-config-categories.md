# Issue 4: Add config-driven starter category list

## Title
Load predefined categories from config for the MVP

## Description
Move the starter category list into a config file so the categories are easy to adjust without changing the code.

## Acceptance Criteria
- Categories are defined in a config file
- The backend loads the category list at runtime
- The list is small and suitable for the MVP
